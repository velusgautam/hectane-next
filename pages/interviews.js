import Layout from '../layouts/main.layout';

import { useRouter } from 'next/router';

const Interviews = () => {
  const router = useRouter();
  // console.log(router);

  return <Layout>Interviews</Layout>;
};

export default Interviews;
